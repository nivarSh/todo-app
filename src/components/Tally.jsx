export function Tally() {

    // Will come back to this, got stuck on props
    // Currently all the logic is stored in Timer.jsx

  return (
    <header>
      <h1 className="text-gradient"></h1>
    </header>
  );
}
